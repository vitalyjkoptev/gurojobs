<?xml version="1.0" encoding="utf-8"?>
<!--
  Identity is bound to the device install. Cloud backup / device transfer
  must NOT carry over auth tokens or cached user prefs — that produces a
  "ghost identity" where a fresh install starts logged in as the previous
  account. See AuthProvider._checkLoginStatus + ApiService for the runtime
  guard; this file is the OS-level backstop.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="file" path="."/>
        <exclude domain="database" path="."/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="file" path="."/>
        <exclude domain="database" path="."/>
    </device-transfer>
</data-extraction-rules>
